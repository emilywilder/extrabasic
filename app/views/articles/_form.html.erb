<%= bootstrap_form_with model: @article, local: true do |form| %>
  <%= form.text_field :title, placeholder: 'Enter title' %>
  <%= form.text_area :text, placeholder: 'Content', size: '60x10' %>
  <%= form.submit 'Submit', class: 'btn btn-primary' %>
<% end %>
